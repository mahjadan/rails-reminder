<%=turbo_stream.remove(dom_id(@reminder.notification, :notification))%>
<% if @destroy == true %>
  <%= turbo_stream.remove(@reminder)%>
<% end %>
<%= turbo_stream.prepend "flash", partial: "layouts/flash" %>
