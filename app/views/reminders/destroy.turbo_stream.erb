<%= turbo_stream.remove(dom_id(@reminder)) %>
<% @reminder.notification.each do |notification| %>
  <%= turbo_stream.remove(dom_id(notification, :notification)) %>
<% end %>
<%= turbo_stream.prepend "flash", partial: "layouts/flash" %>
